# AI Coding Agents Instructions

> **ATTENTION AGENT:** Please refer to [`.agents/context.md`](./.agents/context.md) for full context, tech stack specifications, and prototyping rules for **DYAD**.

## Master Compiler Agent & Official Project Path:
* **Master Compiler Agent:** `Antigravity` (Google DeepMind Advanced Agentic AI Assistant)
* **Official Final Project Folder Path:** `dyad-app/` (`c:/Users/daiwi/Code/DYAD-PRAYAS/dyad-app/`)
* **Sole Compilation Rights:** The **Master Compiler Agent** alone has the exclusive authority to interact with, read, and extract components from all prototype folders in order to synthesize the unified production application in `dyad-app/`.

## Key Multi-Agent Rule for this Hackathon:
To avoid merge conflicts among teammates who use different coding agent platforms:
1. **Never edit shared root files or the production project folder (`dyad-app/`) directly during prototyping.**
2. **Always create and work inside an isolated, self-descriptive prototype subfolder** (e.g., `prototype-agent-orchestration/`, `prototype-datasets-collection/`, `prototype-modal-cloud-experiments/`, `prototype-map-canvas-ui/`).
3. Make sure any other agent or teammate can instantly understand the prototype's purpose purely from its directory name.
4. Export clean, modular functions/components so that the Master Compiler Agent can progressively compile and synthesize them into `dyad-app/`.
5. **Strict Design Craft:** All UI code must strictly adhere to [`.agents/design-system.md`](./.agents/design-system.md) combining **Taste**, **Impeccable** spacing tokens, and **Emil Kowalski** spring micro-interactions on top of **Watermelon UI**. Zero AI slop permitted.

